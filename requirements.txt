# DOMCS-EEG reproducibility — Python dependencies
# Tested with Python 3.10 on Ubuntu 22.04, CUDA 12.x, 8×NVIDIA A100.
torch>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
mne>=1.5          # EEGMMIDB EDF reading / preprocessing
pyedflib>=0.1.30  # alternative EDF reader
tqdm>=4.65
